**This function is used to find the maximum of the array**

<<module arrayMax>>
takes input[list:array[3..20] of real];
returns [max:real];
start 
    declare curr_max:real;
    curr_max:=-10000;
    for(index in 3..20)
    start
        flag:=curr_max<arrayMax[index];
        switch(flag)
            start
                case true: curr:=arrayMax[index];
                break;
                case false: break;
            end
    end
end

<<<driver program>>>
start 
    declare A: array[3..20] of real;
    for(k 
    **ERROR**
        A[k] := k;
    end

    **ERROR** arrayMax with parameters A;
    print(max);
end

